I will scaffold the Personal AI Assistant ("OpenClaw" architecture) with proactive capabilities.

### **Phase 1: Setup & Core Architecture**
1.  **Project Init**: Initialize TypeScript Node.js project.
2.  **Directory Structure**: `src/gateway`, `src/channels`, `src/agents`, `src/skills`, `src/cli`.
3.  **Gateway (Control Plane)**: Implement the central message router and session manager.
4.  **Agent Runner**: Implement the execution loop that can handle **proactive tasks** (cron/wakeups) and reactive messages.

### **Phase 2: Implementation of Key Features**
1.  **CLI Onboarding Wizard**: Create `src/cli/wizard.ts` to interactively setup the workspace, channels, and models.
2.  **Channel Adapter**: Implement a `ConsoleChannel` for immediate testing and a `WebhookChannel` structure for future integrations.
3.  **Soul & Config**: Create `soul.md` and configuration handlers.
4.  **Proactive Loop**: Implement a `Cron` or `Interval` manager in the Gateway to trigger agent actions without user input.

### **Phase 3: Execution**
1.  Run the wizard to generate the config.
2.  Demonstrate the "proactive" nature by having the agent self-trigger a "Welcome" or "Status Check" task upon startup.